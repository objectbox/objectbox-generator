// Code generated by ObjectBox; DO NOT EDIT.
// Learn more about defining entities and generating this file - visit https://golang.objectbox.io/entity-annotations

package object

import (
	"errors"
	"github.com/google/flatbuffers/go"
	"github.com/objectbox/objectbox-go/objectbox"
	"github.com/objectbox/objectbox-go/objectbox/fbutils"
)

type syncedEntity_EntityInfo struct {
	objectbox.Entity
	Uid uint64
}

var SyncedEntityBinding = syncedEntity_EntityInfo{
	Entity: objectbox.Entity{
		Id: 1,
	},
	Uid: 8717895732742165505,
}

// SyncedEntity_ contains type-based Property helpers to facilitate some common operations such as Queries.
var SyncedEntity_ = struct {
	Id            *objectbox.PropertyUint64
	PropertyRel   *objectbox.RelationToOne
	StandaloneRel *objectbox.RelationToMany
}{
	Id: &objectbox.PropertyUint64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     1,
			Entity: &SyncedEntityBinding.Entity,
		},
	},
	PropertyRel: &objectbox.RelationToOne{
		Property: &objectbox.BaseProperty{
			Id:     2,
			Entity: &SyncedEntityBinding.Entity,
		},
		Target: &SyncedRelTargetBinding.Entity,
	},
	StandaloneRel: &objectbox.RelationToMany{
		Id:     1,
		Source: &SyncedEntityBinding.Entity,
		Target: &SyncedRelTargetBinding.Entity,
	},
}

// GeneratorVersion is called by ObjectBox to verify the compatibility of the generator used to generate this code
func (syncedEntity_EntityInfo) GeneratorVersion() int {
	return 6
}

// AddToModel is called by ObjectBox during model build
func (syncedEntity_EntityInfo) AddToModel(model *objectbox.Model) {
	model.Entity("SyncedEntity", 1, 8717895732742165505)
	model.EntityFlags(2)
	model.Property("Id", 6, 1, 6050128673802995827)
	model.PropertyFlags(1)
	model.Property("PropertyRel", 11, 2, 501233450539197794)
	model.PropertyFlags(520)
	model.PropertyRelation("SyncedRelTarget", 1, 3390393562759376202)
	model.EntityLastPropertyId(2, 501233450539197794)
	model.Relation(1, 2669985732393126063, SyncedRelTargetBinding.Id, SyncedRelTargetBinding.Uid)
}

// GetId is called by ObjectBox during Put operations to check for existing ID on an object
func (syncedEntity_EntityInfo) GetId(object interface{}) (uint64, error) {
	return object.(*SyncedEntity).Id, nil
}

// SetId is called by ObjectBox during Put to update an ID on an object that has just been inserted
func (syncedEntity_EntityInfo) SetId(object interface{}, id uint64) error {
	object.(*SyncedEntity).Id = id
	return nil
}

// PutRelated is called by ObjectBox to put related entities before the object itself is flattened and put
func (syncedEntity_EntityInfo) PutRelated(ob *objectbox.ObjectBox, object interface{}, id uint64) error {
	if rel := &object.(*SyncedEntity).PropertyRel; rel != nil {
		if rId, err := SyncedRelTargetBinding.GetId(rel); err != nil {
			return err
		} else if rId == 0 {
			// NOTE Put/PutAsync() has a side-effect of setting the rel.ID
			if _, err := BoxForSyncedRelTarget(ob).Put(rel); err != nil {
				return err
			}
		}
	}
	if err := BoxForSyncedEntity(ob).RelationReplace(SyncedEntity_.StandaloneRel, id, object, object.(*SyncedEntity).StandaloneRel); err != nil {
		return err
	}

	return nil
}

// Flatten is called by ObjectBox to transform an object to a FlatBuffer
func (syncedEntity_EntityInfo) Flatten(object interface{}, fbb *flatbuffers.Builder, id uint64) error {
	obj := object.(*SyncedEntity)

	var rIdPropertyRel uint64
	if rel := &obj.PropertyRel; rel != nil {
		if rId, err := SyncedRelTargetBinding.GetId(rel); err != nil {
			return err
		} else {
			rIdPropertyRel = rId
		}
	}

	// build the FlatBuffers object
	fbb.StartObject(2)
	fbutils.SetUint64Slot(fbb, 0, id)
	fbutils.SetUint64Slot(fbb, 1, rIdPropertyRel)
	return nil
}

// Load is called by ObjectBox to load an object from a FlatBuffer
func (syncedEntity_EntityInfo) Load(ob *objectbox.ObjectBox, bytes []byte) (interface{}, error) {
	if len(bytes) == 0 { // sanity check, should "never" happen
		return nil, errors.New("can't deserialize an object of type 'SyncedEntity' - no data received")
	}

	var table = &flatbuffers.Table{
		Bytes: bytes,
		Pos:   flatbuffers.GetUOffsetT(bytes),
	}

	var propId = table.GetUint64Slot(4, 0)

	var relPropertyRel *SyncedRelTarget
	if rId := fbutils.GetUint64Slot(table, 6); rId > 0 {
		if rObject, err := BoxForSyncedRelTarget(ob).Get(rId); err != nil {
			return nil, err
		} else if rObject == nil {
			relPropertyRel = &SyncedRelTarget{}
		} else {
			relPropertyRel = rObject
		}
	} else {
		relPropertyRel = &SyncedRelTarget{}
	}

	var relStandaloneRel []SyncedRelTarget
	if rIds, err := BoxForSyncedEntity(ob).RelationIds(SyncedEntity_.StandaloneRel, propId); err != nil {
		return nil, err
	} else if rSlice, err := BoxForSyncedRelTarget(ob).GetManyExisting(rIds...); err != nil {
		return nil, err
	} else {
		relStandaloneRel = rSlice
	}

	return &SyncedEntity{
		Id:            propId,
		PropertyRel:   *relPropertyRel,
		StandaloneRel: relStandaloneRel,
	}, nil
}

// MakeSlice is called by ObjectBox to construct a new slice to hold the read objects
func (syncedEntity_EntityInfo) MakeSlice(capacity int) interface{} {
	return make([]*SyncedEntity, 0, capacity)
}

// AppendToSlice is called by ObjectBox to fill the slice of the read objects
func (syncedEntity_EntityInfo) AppendToSlice(slice interface{}, object interface{}) interface{} {
	if object == nil {
		return append(slice.([]*SyncedEntity), nil)
	}
	return append(slice.([]*SyncedEntity), object.(*SyncedEntity))
}

// Box provides CRUD access to SyncedEntity objects
type SyncedEntityBox struct {
	*objectbox.Box
}

// BoxForSyncedEntity opens a box of SyncedEntity objects
func BoxForSyncedEntity(ob *objectbox.ObjectBox) *SyncedEntityBox {
	return &SyncedEntityBox{
		Box: ob.InternalBox(1),
	}
}

// Put synchronously inserts/updates a single object.
// In case the Id is not specified, it would be assigned automatically (auto-increment).
// When inserting, the SyncedEntity.Id property on the passed object will be assigned the new ID as well.
func (box *SyncedEntityBox) Put(object *SyncedEntity) (uint64, error) {
	return box.Box.Put(object)
}

// Insert synchronously inserts a single object. As opposed to Put, Insert will fail if given an ID that already exists.
// In case the Id is not specified, it would be assigned automatically (auto-increment).
// When inserting, the SyncedEntity.Id property on the passed object will be assigned the new ID as well.
func (box *SyncedEntityBox) Insert(object *SyncedEntity) (uint64, error) {
	return box.Box.Insert(object)
}

// Update synchronously updates a single object.
// As opposed to Put, Update will fail if an object with the same ID is not found in the database.
func (box *SyncedEntityBox) Update(object *SyncedEntity) error {
	return box.Box.Update(object)
}

// PutAsync asynchronously inserts/updates a single object.
// Deprecated: use box.Async().Put() instead
func (box *SyncedEntityBox) PutAsync(object *SyncedEntity) (uint64, error) {
	return box.Box.PutAsync(object)
}

// PutMany inserts multiple objects in single transaction.
// In case Ids are not set on the objects, they would be assigned automatically (auto-increment).
//
// Returns: IDs of the put objects (in the same order).
// When inserting, the SyncedEntity.Id property on the objects in the slice will be assigned the new IDs as well.
//
// Note: In case an error occurs during the transaction, some of the objects may already have the SyncedEntity.Id assigned
// even though the transaction has been rolled back and the objects are not stored under those IDs.
//
// Note: The slice may be empty or even nil; in both cases, an empty IDs slice and no error is returned.
func (box *SyncedEntityBox) PutMany(objects []*SyncedEntity) ([]uint64, error) {
	return box.Box.PutMany(objects)
}

// Get reads a single object.
//
// Returns nil (and no error) in case the object with the given ID doesn't exist.
func (box *SyncedEntityBox) Get(id uint64) (*SyncedEntity, error) {
	object, err := box.Box.Get(id)
	if err != nil {
		return nil, err
	} else if object == nil {
		return nil, nil
	}
	return object.(*SyncedEntity), nil
}

// GetMany reads multiple objects at once.
// If any of the objects doesn't exist, its position in the return slice is nil
func (box *SyncedEntityBox) GetMany(ids ...uint64) ([]*SyncedEntity, error) {
	objects, err := box.Box.GetMany(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedEntity), nil
}

// GetManyExisting reads multiple objects at once, skipping those that do not exist.
func (box *SyncedEntityBox) GetManyExisting(ids ...uint64) ([]*SyncedEntity, error) {
	objects, err := box.Box.GetManyExisting(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedEntity), nil
}

// GetAll reads all stored objects
func (box *SyncedEntityBox) GetAll() ([]*SyncedEntity, error) {
	objects, err := box.Box.GetAll()
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedEntity), nil
}

// Remove deletes a single object
func (box *SyncedEntityBox) Remove(object *SyncedEntity) error {
	return box.Box.Remove(object)
}

// RemoveMany deletes multiple objects at once.
// Returns the number of deleted object or error on failure.
// Note that this method will not fail if an object is not found (e.g. already removed).
// In case you need to strictly check whether all of the objects exist before removing them,
// you can execute multiple box.Contains() and box.Remove() inside a single write transaction.
func (box *SyncedEntityBox) RemoveMany(objects ...*SyncedEntity) (uint64, error) {
	var ids = make([]uint64, len(objects))
	for k, object := range objects {
		ids[k] = object.Id
	}
	return box.Box.RemoveIds(ids...)
}

// Creates a query with the given conditions. Use the fields of the SyncedEntity_ struct to create conditions.
// Keep the *SyncedEntityQuery if you intend to execute the query multiple times.
// Note: this function panics if you try to create illegal queries; e.g. use properties of an alien type.
// This is typically a programming error. Use QueryOrError instead if you want the explicit error check.
func (box *SyncedEntityBox) Query(conditions ...objectbox.Condition) *SyncedEntityQuery {
	return &SyncedEntityQuery{
		box.Box.Query(conditions...),
	}
}

// Creates a query with the given conditions. Use the fields of the SyncedEntity_ struct to create conditions.
// Keep the *SyncedEntityQuery if you intend to execute the query multiple times.
func (box *SyncedEntityBox) QueryOrError(conditions ...objectbox.Condition) (*SyncedEntityQuery, error) {
	if query, err := box.Box.QueryOrError(conditions...); err != nil {
		return nil, err
	} else {
		return &SyncedEntityQuery{query}, nil
	}
}

// Async provides access to the default Async Box for asynchronous operations. See SyncedEntityAsyncBox for more information.
func (box *SyncedEntityBox) Async() *SyncedEntityAsyncBox {
	return &SyncedEntityAsyncBox{AsyncBox: box.Box.Async()}
}

// SyncedEntityAsyncBox provides asynchronous operations on SyncedEntity objects.
//
// Asynchronous operations are executed on a separate internal thread for better performance.
//
// There are two main use cases:
//
// 1) "execute & forget:" you gain faster put/remove operations as you don't have to wait for the transaction to finish.
//
// 2) Many small transactions: if your write load is typically a lot of individual puts that happen in parallel,
// this will merge small transactions into bigger ones. This results in a significant gain in overall throughput.
//
// In situations with (extremely) high async load, an async method may be throttled (~1ms) or delayed up to 1 second.
// In the unlikely event that the object could still not be enqueued (full queue), an error will be returned.
//
// Note that async methods do not give you hard durability guarantees like the synchronous Box provides.
// There is a small time window in which the data may not have been committed durably yet.
type SyncedEntityAsyncBox struct {
	*objectbox.AsyncBox
}

// AsyncBoxForSyncedEntity creates a new async box with the given operation timeout in case an async queue is full.
// The returned struct must be freed explicitly using the Close() method.
// It's usually preferable to use SyncedEntityBox::Async() which takes care of resource management and doesn't require closing.
func AsyncBoxForSyncedEntity(ob *objectbox.ObjectBox, timeoutMs uint64) *SyncedEntityAsyncBox {
	var async, err = objectbox.NewAsyncBox(ob, 1, timeoutMs)
	if err != nil {
		panic("Could not create async box for entity ID 1: %s" + err.Error())
	}
	return &SyncedEntityAsyncBox{AsyncBox: async}
}

// Put inserts/updates a single object asynchronously.
// When inserting a new object, the Id property on the passed object will be assigned the new ID the entity would hold
// if the insert is ultimately successful. The newly assigned ID may not become valid if the insert fails.
func (asyncBox *SyncedEntityAsyncBox) Put(object *SyncedEntity) (uint64, error) {
	return asyncBox.AsyncBox.Put(object)
}

// Insert a single object asynchronously.
// The Id property on the passed object will be assigned the new ID the entity would hold if the insert is ultimately
// successful. The newly assigned ID may not become valid if the insert fails.
// Fails silently if an object with the same ID already exists (this error is not returned).
func (asyncBox *SyncedEntityAsyncBox) Insert(object *SyncedEntity) (id uint64, err error) {
	return asyncBox.AsyncBox.Insert(object)
}

// Update a single object asynchronously.
// The object must already exists or the update fails silently (without an error returned).
func (asyncBox *SyncedEntityAsyncBox) Update(object *SyncedEntity) error {
	return asyncBox.AsyncBox.Update(object)
}

// Remove deletes a single object asynchronously.
func (asyncBox *SyncedEntityAsyncBox) Remove(object *SyncedEntity) error {
	return asyncBox.AsyncBox.Remove(object)
}

// Query provides a way to search stored objects
//
// For example, you can find all SyncedEntity which Id is either 42 or 47:
//
// box.Query(SyncedEntity_.Id.In(42, 47)).Find()
type SyncedEntityQuery struct {
	*objectbox.Query
}

// Find returns all objects matching the query
func (query *SyncedEntityQuery) Find() ([]*SyncedEntity, error) {
	objects, err := query.Query.Find()
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedEntity), nil
}

// Offset defines the index of the first object to process (how many objects to skip)
func (query *SyncedEntityQuery) Offset(offset uint64) *SyncedEntityQuery {
	query.Query.Offset(offset)
	return query
}

// Limit sets the number of elements to process by the query
func (query *SyncedEntityQuery) Limit(limit uint64) *SyncedEntityQuery {
	query.Query.Limit(limit)
	return query
}

type syncedRelTarget_EntityInfo struct {
	objectbox.Entity
	Uid uint64
}

var SyncedRelTargetBinding = syncedRelTarget_EntityInfo{
	Entity: objectbox.Entity{
		Id: 2,
	},
	Uid: 2259404117704393152,
}

// SyncedRelTarget_ contains type-based Property helpers to facilitate some common operations such as Queries.
var SyncedRelTarget_ = struct {
	Id *objectbox.PropertyUint64
}{
	Id: &objectbox.PropertyUint64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     1,
			Entity: &SyncedRelTargetBinding.Entity,
		},
	},
}

// GeneratorVersion is called by ObjectBox to verify the compatibility of the generator used to generate this code
func (syncedRelTarget_EntityInfo) GeneratorVersion() int {
	return 6
}

// AddToModel is called by ObjectBox during model build
func (syncedRelTarget_EntityInfo) AddToModel(model *objectbox.Model) {
	model.Entity("SyncedRelTarget", 2, 2259404117704393152)
	model.EntityFlags(2)
	model.Property("Id", 6, 1, 1774932891286980153)
	model.PropertyFlags(1)
	model.EntityLastPropertyId(1, 1774932891286980153)
}

// GetId is called by ObjectBox during Put operations to check for existing ID on an object
func (syncedRelTarget_EntityInfo) GetId(object interface{}) (uint64, error) {
	return object.(*SyncedRelTarget).Id, nil
}

// SetId is called by ObjectBox during Put to update an ID on an object that has just been inserted
func (syncedRelTarget_EntityInfo) SetId(object interface{}, id uint64) error {
	object.(*SyncedRelTarget).Id = id
	return nil
}

// PutRelated is called by ObjectBox to put related entities before the object itself is flattened and put
func (syncedRelTarget_EntityInfo) PutRelated(ob *objectbox.ObjectBox, object interface{}, id uint64) error {
	return nil
}

// Flatten is called by ObjectBox to transform an object to a FlatBuffer
func (syncedRelTarget_EntityInfo) Flatten(object interface{}, fbb *flatbuffers.Builder, id uint64) error {

	// build the FlatBuffers object
	fbb.StartObject(1)
	fbutils.SetUint64Slot(fbb, 0, id)
	return nil
}

// Load is called by ObjectBox to load an object from a FlatBuffer
func (syncedRelTarget_EntityInfo) Load(ob *objectbox.ObjectBox, bytes []byte) (interface{}, error) {
	if len(bytes) == 0 { // sanity check, should "never" happen
		return nil, errors.New("can't deserialize an object of type 'SyncedRelTarget' - no data received")
	}

	var table = &flatbuffers.Table{
		Bytes: bytes,
		Pos:   flatbuffers.GetUOffsetT(bytes),
	}

	var propId = table.GetUint64Slot(4, 0)

	return &SyncedRelTarget{
		Id: propId,
	}, nil
}

// MakeSlice is called by ObjectBox to construct a new slice to hold the read objects
func (syncedRelTarget_EntityInfo) MakeSlice(capacity int) interface{} {
	return make([]*SyncedRelTarget, 0, capacity)
}

// AppendToSlice is called by ObjectBox to fill the slice of the read objects
func (syncedRelTarget_EntityInfo) AppendToSlice(slice interface{}, object interface{}) interface{} {
	if object == nil {
		return append(slice.([]*SyncedRelTarget), nil)
	}
	return append(slice.([]*SyncedRelTarget), object.(*SyncedRelTarget))
}

// Box provides CRUD access to SyncedRelTarget objects
type SyncedRelTargetBox struct {
	*objectbox.Box
}

// BoxForSyncedRelTarget opens a box of SyncedRelTarget objects
func BoxForSyncedRelTarget(ob *objectbox.ObjectBox) *SyncedRelTargetBox {
	return &SyncedRelTargetBox{
		Box: ob.InternalBox(2),
	}
}

// Put synchronously inserts/updates a single object.
// In case the Id is not specified, it would be assigned automatically (auto-increment).
// When inserting, the SyncedRelTarget.Id property on the passed object will be assigned the new ID as well.
func (box *SyncedRelTargetBox) Put(object *SyncedRelTarget) (uint64, error) {
	return box.Box.Put(object)
}

// Insert synchronously inserts a single object. As opposed to Put, Insert will fail if given an ID that already exists.
// In case the Id is not specified, it would be assigned automatically (auto-increment).
// When inserting, the SyncedRelTarget.Id property on the passed object will be assigned the new ID as well.
func (box *SyncedRelTargetBox) Insert(object *SyncedRelTarget) (uint64, error) {
	return box.Box.Insert(object)
}

// Update synchronously updates a single object.
// As opposed to Put, Update will fail if an object with the same ID is not found in the database.
func (box *SyncedRelTargetBox) Update(object *SyncedRelTarget) error {
	return box.Box.Update(object)
}

// PutAsync asynchronously inserts/updates a single object.
// Deprecated: use box.Async().Put() instead
func (box *SyncedRelTargetBox) PutAsync(object *SyncedRelTarget) (uint64, error) {
	return box.Box.PutAsync(object)
}

// PutMany inserts multiple objects in single transaction.
// In case Ids are not set on the objects, they would be assigned automatically (auto-increment).
//
// Returns: IDs of the put objects (in the same order).
// When inserting, the SyncedRelTarget.Id property on the objects in the slice will be assigned the new IDs as well.
//
// Note: In case an error occurs during the transaction, some of the objects may already have the SyncedRelTarget.Id assigned
// even though the transaction has been rolled back and the objects are not stored under those IDs.
//
// Note: The slice may be empty or even nil; in both cases, an empty IDs slice and no error is returned.
func (box *SyncedRelTargetBox) PutMany(objects []*SyncedRelTarget) ([]uint64, error) {
	return box.Box.PutMany(objects)
}

// Get reads a single object.
//
// Returns nil (and no error) in case the object with the given ID doesn't exist.
func (box *SyncedRelTargetBox) Get(id uint64) (*SyncedRelTarget, error) {
	object, err := box.Box.Get(id)
	if err != nil {
		return nil, err
	} else if object == nil {
		return nil, nil
	}
	return object.(*SyncedRelTarget), nil
}

// GetMany reads multiple objects at once.
// If any of the objects doesn't exist, its position in the return slice is nil
func (box *SyncedRelTargetBox) GetMany(ids ...uint64) ([]*SyncedRelTarget, error) {
	objects, err := box.Box.GetMany(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedRelTarget), nil
}

// GetManyExisting reads multiple objects at once, skipping those that do not exist.
func (box *SyncedRelTargetBox) GetManyExisting(ids ...uint64) ([]*SyncedRelTarget, error) {
	objects, err := box.Box.GetManyExisting(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedRelTarget), nil
}

// GetAll reads all stored objects
func (box *SyncedRelTargetBox) GetAll() ([]*SyncedRelTarget, error) {
	objects, err := box.Box.GetAll()
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedRelTarget), nil
}

// Remove deletes a single object
func (box *SyncedRelTargetBox) Remove(object *SyncedRelTarget) error {
	return box.Box.Remove(object)
}

// RemoveMany deletes multiple objects at once.
// Returns the number of deleted object or error on failure.
// Note that this method will not fail if an object is not found (e.g. already removed).
// In case you need to strictly check whether all of the objects exist before removing them,
// you can execute multiple box.Contains() and box.Remove() inside a single write transaction.
func (box *SyncedRelTargetBox) RemoveMany(objects ...*SyncedRelTarget) (uint64, error) {
	var ids = make([]uint64, len(objects))
	for k, object := range objects {
		ids[k] = object.Id
	}
	return box.Box.RemoveIds(ids...)
}

// Creates a query with the given conditions. Use the fields of the SyncedRelTarget_ struct to create conditions.
// Keep the *SyncedRelTargetQuery if you intend to execute the query multiple times.
// Note: this function panics if you try to create illegal queries; e.g. use properties of an alien type.
// This is typically a programming error. Use QueryOrError instead if you want the explicit error check.
func (box *SyncedRelTargetBox) Query(conditions ...objectbox.Condition) *SyncedRelTargetQuery {
	return &SyncedRelTargetQuery{
		box.Box.Query(conditions...),
	}
}

// Creates a query with the given conditions. Use the fields of the SyncedRelTarget_ struct to create conditions.
// Keep the *SyncedRelTargetQuery if you intend to execute the query multiple times.
func (box *SyncedRelTargetBox) QueryOrError(conditions ...objectbox.Condition) (*SyncedRelTargetQuery, error) {
	if query, err := box.Box.QueryOrError(conditions...); err != nil {
		return nil, err
	} else {
		return &SyncedRelTargetQuery{query}, nil
	}
}

// Async provides access to the default Async Box for asynchronous operations. See SyncedRelTargetAsyncBox for more information.
func (box *SyncedRelTargetBox) Async() *SyncedRelTargetAsyncBox {
	return &SyncedRelTargetAsyncBox{AsyncBox: box.Box.Async()}
}

// SyncedRelTargetAsyncBox provides asynchronous operations on SyncedRelTarget objects.
//
// Asynchronous operations are executed on a separate internal thread for better performance.
//
// There are two main use cases:
//
// 1) "execute & forget:" you gain faster put/remove operations as you don't have to wait for the transaction to finish.
//
// 2) Many small transactions: if your write load is typically a lot of individual puts that happen in parallel,
// this will merge small transactions into bigger ones. This results in a significant gain in overall throughput.
//
// In situations with (extremely) high async load, an async method may be throttled (~1ms) or delayed up to 1 second.
// In the unlikely event that the object could still not be enqueued (full queue), an error will be returned.
//
// Note that async methods do not give you hard durability guarantees like the synchronous Box provides.
// There is a small time window in which the data may not have been committed durably yet.
type SyncedRelTargetAsyncBox struct {
	*objectbox.AsyncBox
}

// AsyncBoxForSyncedRelTarget creates a new async box with the given operation timeout in case an async queue is full.
// The returned struct must be freed explicitly using the Close() method.
// It's usually preferable to use SyncedRelTargetBox::Async() which takes care of resource management and doesn't require closing.
func AsyncBoxForSyncedRelTarget(ob *objectbox.ObjectBox, timeoutMs uint64) *SyncedRelTargetAsyncBox {
	var async, err = objectbox.NewAsyncBox(ob, 2, timeoutMs)
	if err != nil {
		panic("Could not create async box for entity ID 2: %s" + err.Error())
	}
	return &SyncedRelTargetAsyncBox{AsyncBox: async}
}

// Put inserts/updates a single object asynchronously.
// When inserting a new object, the Id property on the passed object will be assigned the new ID the entity would hold
// if the insert is ultimately successful. The newly assigned ID may not become valid if the insert fails.
func (asyncBox *SyncedRelTargetAsyncBox) Put(object *SyncedRelTarget) (uint64, error) {
	return asyncBox.AsyncBox.Put(object)
}

// Insert a single object asynchronously.
// The Id property on the passed object will be assigned the new ID the entity would hold if the insert is ultimately
// successful. The newly assigned ID may not become valid if the insert fails.
// Fails silently if an object with the same ID already exists (this error is not returned).
func (asyncBox *SyncedRelTargetAsyncBox) Insert(object *SyncedRelTarget) (id uint64, err error) {
	return asyncBox.AsyncBox.Insert(object)
}

// Update a single object asynchronously.
// The object must already exists or the update fails silently (without an error returned).
func (asyncBox *SyncedRelTargetAsyncBox) Update(object *SyncedRelTarget) error {
	return asyncBox.AsyncBox.Update(object)
}

// Remove deletes a single object asynchronously.
func (asyncBox *SyncedRelTargetAsyncBox) Remove(object *SyncedRelTarget) error {
	return asyncBox.AsyncBox.Remove(object)
}

// Query provides a way to search stored objects
//
// For example, you can find all SyncedRelTarget which Id is either 42 or 47:
//
// box.Query(SyncedRelTarget_.Id.In(42, 47)).Find()
type SyncedRelTargetQuery struct {
	*objectbox.Query
}

// Find returns all objects matching the query
func (query *SyncedRelTargetQuery) Find() ([]*SyncedRelTarget, error) {
	objects, err := query.Query.Find()
	if err != nil {
		return nil, err
	}
	return objects.([]*SyncedRelTarget), nil
}

// Offset defines the index of the first object to process (how many objects to skip)
func (query *SyncedRelTargetQuery) Offset(offset uint64) *SyncedRelTargetQuery {
	query.Query.Offset(offset)
	return query
}

// Limit sets the number of elements to process by the query
func (query *SyncedRelTargetQuery) Limit(limit uint64) *SyncedRelTargetQuery {
	query.Query.Limit(limit)
	return query
}
